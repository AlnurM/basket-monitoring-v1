---
phase: 04-analytics-charts-and-export
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/price_spy/services/charts.py
  - src/price_spy/services/export.py
  - src/price_spy/services/alerts.py
autonomous: true
requirements:
  - CHRT-01
  - CHRT-02
  - CHRT-03
  - CHRT-04
  - CHRT-05
  - ALRT-01
  - ALRT-02
  - EXPT-01
  - EXPT-02
  - EXPT-03

must_haves:
  truths:
    - "generate_basket_chart() returns PNG bytes of a line chart showing basket total over time"
    - "generate_item_chart() returns PNG bytes with green discount dots and red X out-of-stock markers"
    - "generate_csv_bytes() returns UTF-8 bytes with BOM containing date,basket,source,product,quantity,unit_price,total,available columns"
    - "check_and_send_alerts() finds products with >10% price drop and sends bilingual alert messages"
  artifacts:
    - path: "src/price_spy/services/charts.py"
      provides: "Chart generation with matplotlib in run_in_executor"
      contains: "run_in_executor"
    - path: "src/price_spy/services/export.py"
      provides: "CSV export with BOM"
      contains: "\\\\xef\\\\xbb\\\\xbf"
    - path: "src/price_spy/services/alerts.py"
      provides: "Price drop alert detection and sending"
      contains: "check_and_send_alerts"
  key_links:
    - from: "src/price_spy/services/charts.py"
      to: "matplotlib"
      via: "matplotlib.use('Agg') before pyplot import"
      pattern: "matplotlib.use.*Agg"
    - from: "src/price_spy/services/alerts.py"
      to: "src/price_spy/db/repositories/price_history.py"
      via: "get_previous_prices for old price comparison"
      pattern: "get_previous_prices"
---

<objective>
Build chart generation, CSV export, and price drop alert service modules.

Purpose: Provide the business logic for /chart, /chart_item, /export commands and automatic price drop notifications. Charts use matplotlib with run_in_executor to avoid blocking. CSV includes UTF-8 BOM for Excel/Cyrillic compatibility. Alerts detect >10% price drops after daily scrape.
Output: Three service modules (charts.py, export.py, alerts.py).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-analytics-charts-and-export/04-CONTEXT.md
@.planning/phases/04-analytics-charts-and-export/04-RESEARCH.md

@src/price_spy/services/report.py
@src/price_spy/services/daily_scrape.py
@src/price_spy/db/repositories/price_history.py
@src/price_spy/db/repositories/basket_item.py
@src/price_spy/db/models/price_history.py

<interfaces>
<!-- From report.py — pattern to follow -->
```python
REPORT_STRINGS: dict[str, dict[str, str]]  # Bilingual strings pattern
def _get_str(lang: str, key: str) -> str
def _format_number(value: Decimal | int) -> str
```

<!-- PriceHistory model -->
```python
class PriceHistory(Base):
    id: Mapped[int]
    basket_item_id: Mapped[int]
    price: Mapped[Decimal | None]
    original_price: Mapped[Decimal | None]
    is_available: Mapped[bool]
    scraped_at: Mapped[datetime.datetime]
    basket_item: Mapped["BasketItem"]  # relationship
```

<!-- BasketItem model -->
```python
class BasketItem(Base):
    id: Mapped[int]
    basket_id: Mapped[int]
    product_url: Mapped[str]
    product_id: Mapped[str]
    url_source: Mapped[str]
    name: Mapped[str | None]
    quantity: Mapped[int]
```

<!-- PriceHistoryRepository (will be extended by Plan 01) -->
```python
async def get_price_range(self, basket_item_ids, start_date, end_date) -> list[PriceHistory]
async def get_daily_basket_totals(self, basket_item_ids, quantities, start_date, end_date) -> list[tuple[date, Decimal]]
async def get_export_data(self, basket_item_ids, start_date, end_date) -> list[PriceHistory]
async def get_previous_prices(self, basket_item_ids, before) -> dict[int, tuple[Decimal|None, bool]]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chart generation service with matplotlib</name>
  <files>src/price_spy/services/charts.py</files>
  <read_first>
    - src/price_spy/services/report.py (REPORT_STRINGS pattern, _format_number)
    - src/price_spy/db/repositories/price_history.py (get_price_range, get_daily_basket_totals signatures)
    - src/price_spy/db/repositories/basket_item.py (get_basket_items_with_latest_price, get_items_by_basket)
    - task.md lines 362-373 (section 7.3 chart specs)
  </read_first>
  <action>
Create src/price_spy/services/charts.py following the research Pattern 2 exactly.

CRITICAL requirements:
- `matplotlib.use('Agg')` MUST be the FIRST matplotlib call, BEFORE `import matplotlib.pyplot as plt`.
- All chart generation MUST use `asyncio.get_event_loop().run_in_executor()` to avoid blocking the event loop.
- Every figure MUST be closed with `plt.close(fig)` in a finally block to prevent memory leaks.

1. Define CHART_STRINGS bilingual dict with keys:
   - "chart_title_basket": "Стоимость корзины: {name}" / "Basket cost: {name}"
   - "chart_title_item": "Цена: {name}" / "Price: {name}"
   - "chart_ylabel": "Цена (₸)" / "Price (₸)"
   - "chart_no_data": "Недостаточно данных для графика. Попробуйте после следующего парсинга." / "Not enough data for chart. Try after next scrape."
   - "chart_generating": "Генерирую график..." / "Generating chart..."

2. Implement `_generate_basket_chart_sync(dates: list[str], totals: list[float], title: str, ylabel: str) -> bytes`:
   - Creates figure with figsize=(10, 5), dpi=100.
   - Line plot with marker='o', linewidth=2, color='#2196F3'.
   - Title, ylabel from params. Grid with alpha=0.3.
   - Y-axis formatter: `'{int(x):,} ₸'.replace(',', ' ')` for space-separated thousands with tenge symbol (per D-10).
   - `fig.autofmt_xdate(rotation=45)` for readable date labels.
   - `fig.tight_layout()`.
   - Save to BytesIO as PNG, close figure, return bytes.

3. Implement `async def generate_basket_chart(session, basket_id, basket_name, days, lang) -> bytes | None`:
   - Get items via BasketItemRepository.get_items_by_basket(basket_id).
   - Build quantities dict {item.id: item.quantity}.
   - Compute start_date = now - timedelta(days=days) in Asia/Almaty.
   - Call PriceHistoryRepository.get_daily_basket_totals(item_ids, quantities, start_date, now).
   - If less than 2 data points, return None.
   - Format dates as DD.MM strings.
   - Build title from CHART_STRINGS using basket_name.
   - Call `await asyncio.get_event_loop().run_in_executor(None, _generate_basket_chart_sync, dates, totals, title, ylabel)`.
   - Return PNG bytes.

4. Implement `_generate_item_chart_sync(dates, prices, original_prices, availabilities, title, ylabel) -> bytes`:
   - Line plot of prices over time (per D-08).
   - Green dots (color='#4CAF50', marker='o', zorder=5) where original_price is not None AND original_price > price (discount).
   - Red X markers (color='#F44336', marker='x', s=100, zorder=5) where is_available=False (per D-08, CHRT-04).
   - Same styling as basket chart (grid, ylabel formatter, tight_layout).
   - Close figure in finally block.

5. Implement `async def generate_item_chart(session, basket_id, item_index, days, lang) -> tuple[bytes | None, str | None]`:
   - Get items via get_items_by_basket(basket_id), sorted by created_at.
   - Validate item_index is within range (1-based). If not, return (None, error_message).
   - Get price history for that item via get_price_range([item_id], start_date, now).
   - If less than 2 records, return (None, no_data_message).
   - Extract dates, prices, original_prices, availabilities from records.
   - Build title from item name.
   - Call run_in_executor with _generate_item_chart_sync.
   - Return (PNG bytes, None).

Default period: 30 days per D-09. User can specify custom via command args (handled in handler, not here).
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/product-basket-v1 && python -c "
import matplotlib
matplotlib.use('Agg')
from price_spy.services.charts import generate_basket_chart, generate_item_chart, CHART_STRINGS, _generate_basket_chart_sync
# Verify sync function produces bytes
result = _generate_basket_chart_sync(['01.01', '02.01', '03.01'], [1000.0, 1100.0, 1050.0], 'Test', 'Price')
assert isinstance(result, bytes), f'Expected bytes, got {type(result)}'
assert len(result) > 1000, 'PNG too small'
print('charts OK, PNG size:', len(result))
"</automated>
  </verify>
  <acceptance_criteria>
    - src/price_spy/services/charts.py exists
    - File starts with `matplotlib.use('Agg')` before any pyplot import
    - Contains generate_basket_chart async function using run_in_executor
    - Contains generate_item_chart async function using run_in_executor
    - _generate_basket_chart_sync produces valid PNG bytes
    - _generate_item_chart_sync marks discount prices with green dots and out-of-stock with red X
    - Every plt.subplots call has a corresponding plt.close(fig) in finally block
    - CHART_STRINGS dict has both "ru" and "en" keys
  </acceptance_criteria>
  <done>charts.py generates basket total line charts and individual item charts as PNG bytes. Charts use run_in_executor (CHRT-05), support custom period (CHRT-03), mark discounts green and out-of-stock red (CHRT-04), and have bilingual titles (D-10).</done>
</task>

<task type="auto">
  <name>Task 2: Create CSV export service and price drop alert service</name>
  <files>src/price_spy/services/export.py, src/price_spy/services/alerts.py</files>
  <read_first>
    - src/price_spy/services/report.py (bilingual strings pattern)
    - src/price_spy/services/daily_scrape.py (scrape_all_baskets flow, understand where alerts hook in)
    - src/price_spy/db/repositories/price_history.py (get_export_data, get_previous_prices)
    - src/price_spy/db/repositories/basket_item.py (get_items_by_basket)
    - src/price_spy/db/repositories/basket.py (get_all_active_baskets, get_user_baskets)
    - src/price_spy/__main__.py (deliver_reports._bot pattern for sending from scheduler)
    - task.md lines 375-382 (section 7.4 CSV format)
  </read_first>
  <action>
**Create src/price_spy/services/export.py** (per D-14, D-15, D-16):

1. Define EXPORT_STRINGS bilingual dict:
   - "export_no_data": "Нет данных для экспорта." / "No data to export."
   - "export_generating": "Генерирую CSV..." / "Generating CSV..."

2. Implement `def generate_csv_bytes(rows: list, basket_name: str, source: str) -> bytes`:
   - Uses csv.writer on io.StringIO.
   - Header row: `["date", "basket", "source", "product", "quantity", "unit_price", "total", "available"]` per D-15 / EXPT-02.
   - Each row: date formatted as YYYY-MM-DD, basket_name, source, product name, quantity, unit_price, unit_price * quantity, "true"/"false" for availability.
   - Prepend UTF-8 BOM `b'\xef\xbb\xbf'` to output bytes per D-16 / EXPT-03.
   - Return bytes.

3. Implement `async def generate_export(session, basket_id, basket_name, source, lang) -> bytes | None`:
   - Get items via get_items_by_basket(basket_id).
   - If no items, return None.
   - Get all price history via get_export_data(item_ids, start_date=90_days_ago, end_date=now).
   - Build row dicts from PriceHistory records (accessing basket_item relationship for name, quantity).
   - Call generate_csv_bytes.
   - Return bytes.

**Create src/price_spy/services/alerts.py** (per D-11, D-12, D-13):

1. Define ALERT_STRINGS bilingual dict:
   - "alert_price_drop": "🟢 {name} подешевел(а) на {pct}%: {old} ₸ → {new} ₸" / "🟢 {name} dropped by {pct}%: {old} ₸ → {new} ₸"

2. Implement `async def check_and_send_alerts(bot) -> None`:
   - Opens its own async_session (self-managed, same pattern as daily_scrape/deliver_reports).
   - Get all active baskets via BasketRepository.get_all_active_baskets().
   - Group baskets by user_id.
   - For each user's baskets:
     - Get all basket items across user's baskets.
     - Get today's prices (latest) and yesterday's prices using get_previous_prices with today_start as cutoff.
     - Compare: if new_price < old_price * 0.9 (>10% drop per D-11), add to alerts list.
     - If any alerts for this user:
       - Get user's language from User model.
       - Batch all alerts into a single message (per research recommendation to reduce API calls).
       - Format each alert line using ALERT_STRINGS per D-12 / ALRT-02.
       - Send via bot.send_message(chat_id=user.telegram_id, text=alert_message).
       - Handle TelegramForbiddenError (user blocked bot) and TelegramRetryAfter (rate limit).
       - `await asyncio.sleep(1)` between users for flood control (same pattern as deliver_reports).
   - Log alert count.

Import from price_spy.db.engine import async_session at top (not inside function — follows existing pattern in daily_scrape.py).
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/product-basket-v1 && python -c "
from price_spy.services.export import generate_csv_bytes, EXPORT_STRINGS
# Test BOM and header
test_bytes = generate_csv_bytes([], 'TestBasket', 'arbuz')
assert test_bytes[:3] == b'\xef\xbb\xbf', 'Missing UTF-8 BOM'
content = test_bytes.decode('utf-8-sig')
assert 'date,basket,source,product,quantity,unit_price,total,available' in content, 'Missing CSV header'
print('export OK')
from price_spy.services.alerts import check_and_send_alerts, ALERT_STRINGS
assert 'ru' in ALERT_STRINGS and 'en' in ALERT_STRINGS
print('alerts OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - src/price_spy/services/export.py exists with generate_csv_bytes and generate_export functions
    - generate_csv_bytes produces bytes starting with BOM (b'\xef\xbb\xbf')
    - CSV header matches: date,basket,source,product,quantity,unit_price,total,available
    - src/price_spy/services/alerts.py exists with check_and_send_alerts function
    - Alerts detect >10% price drops (new_price < old_price * 0.9)
    - Alert messages include product name, old price, new price, percentage per ALRT-02
    - Both modules have bilingual STRINGS dicts with "ru" and "en" keys
    - alerts.py uses self-managed async_session pattern (opens its own session)
    - alerts.py handles TelegramForbiddenError and TelegramRetryAfter
  </acceptance_criteria>
  <done>export.py generates UTF-8-BOM CSV with correct columns per EXPT-01..03. alerts.py detects >10% price drops per ALRT-01 and sends batched bilingual alerts per user per ALRT-02.</done>
</task>

</tasks>

<verification>
- `python -c "from price_spy.services.charts import generate_basket_chart, generate_item_chart"` succeeds
- `python -c "from price_spy.services.export import generate_csv_bytes"` succeeds
- `python -c "from price_spy.services.alerts import check_and_send_alerts"` succeeds
- _generate_basket_chart_sync produces valid PNG bytes when given test data
- generate_csv_bytes returns bytes with BOM prefix
</verification>

<success_criteria>
- charts.py generates matplotlib PNG charts in thread executor, with discount/out-of-stock markers
- export.py generates UTF-8-BOM CSV matching task.md section 7.4 format
- alerts.py detects >10% price drops and sends batched per-user alerts
- All three modules follow the bilingual STRINGS dict pattern from report.py
</success_criteria>

<output>
After completion, create `.planning/phases/04-analytics-charts-and-export/04-02-SUMMARY.md`
</output>
